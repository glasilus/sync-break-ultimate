# **sYnC-B r E a K	u L t i M a T e**: Generative Video Montage Engine

## 🚨 Введение и Установка

>**синк-брейк-УЛЬТИМЕЙТ** — это Python-движок для алгоритмического монтажа, основанный на детальном анализе аудиодорожки и управлении видеоэффектами.

### 1. Requirements.txt

Для корректной работы требуется **Python версии 3.10.x** (проверены версии от 3.8 до 3.10).
Использование более новых версий Python (3.12+) может привести к ошибкам при установке бинарных зависимостей (Numpy/Scipy).

```text
moviepy==1.0.3                 
librosa==0.9.2                 
numpy==1.24.4                  
scipy==1.10.1                  
soundfile==0.12.1              
opencv-python==4.9.0.80        
Pillow==10.1.0                 
scenedetect[opencv]==0.6.0     
imageio-ffmpeg==0.4.9  

```

**Установка:** `pip install -r requirements.txt`

### 2. Запуск

Разместите `engine.py` и `gui.py` в одной директории и запустите: `python gui.py`

(или же воспользуйтесь .exe файлом в релизах)

## II. Основа: Аудио-Анализ и Сегментация

Движок делит видео на сегменты, используя **Onsets** (обнаружение ударов). Каждый сегмент анализируется по трем метрикам для активации эффектов.

| Метрика | Анализируемый параметр | Роль в активации FX |
| --- | --- | --- |
| **RMS (Root Mean Square)** | Среднеквадратичная громкость. | Определяет **громкость** сегмента относительно среднего уровня (`Beat Threshold`). |
| **Spectral Flatness** | Отношение геометрического среднего спектра к арифметическому среднему. | Определяет, является ли звук **"шумом"** или чистым тоном. Триггер для **Pixel Sort**. |
| **RMS Change** | Резкое падение RMS между соседними сегментами. | Триггер для **Flash Frame**. |

---

## III. Детальный Разбор Настроек GUI

### A. Вкладка I: Cut Logic (Алгоритм Монтажа)

Контролирует базовую реактивность и логику выбора видеоматериала.

| Настройка | Диапазон | Логика влияния | Рекомендации |
| --- | --- | --- | --- |
| **Beat Threshold** | 0.5 – 2.0 | Если , сегмент классифицируется как **громкий**. Определяет **чувствительность** к ударам. | Уменьшайте для реагирования на низкую громкость; увеличивайте для акцента только на самых сильных ударах. |
| **Min Cut Duration** | 0.0 – 0.2 сек | Все сегменты короче этого значения игнорируются, предотвращая микро-глитчи в тихих местах. | Для самого быстрого монтажа используйте 0.04-0.05 с. |
| **Smart Scene Detection** | On/Off | При ON, клипы для монтажа берутся, начиная с точек перехода сцен (через PySceneDetect), повышая визуальную связность. |  |
| **Scene Buffer Size** | 2 – 30 | Количество последних уникальных сцен, которые могут быть использованы для рандомной нарезки в хаотичные моменты (`Global Chaos` активен). | Высокое значение обеспечивает максимальное перемешивание исходного материала. |
| **Global Chaos** | 0.0 – 1.0 | Общий коэффициент рандомизации в выборе клипов (в обход `Scene Detection`) и повышения вероятности всех FX. |  |

### B. Вкладка II: Datamix Core (Глитч-Симуляция)

Эффекты, привязанные к "шумовым" характеристикам аудио.

| Настройка | Тип | Условие срабатывания | Логика влияния |
| --- | --- | --- | --- |
| **Pixel Sort Simulation** | On/Off | Если  и `Chance` прошла. | Активирует функцию `fx_pixel_sort`, сортируя пиксели по яркости для имитации сбоя кодека. |
| **Pixel Sort Chance** | 0.0 – 1.0 | — | Вероятность, с которой эффект будет применён, если выполнено условие **"шума"**. |
| **Pixel Sort Intensity** | 0.0 – 1.0 | — | Определяет размер области кадра, подвергаемой сортировке. |

### C. Вкладка III: Rhythm FX (Ритмические Эффекты)

| Настройка | Тип | Условие срабатывания | Логика влияния |
| --- | --- | --- | --- |
| **Stutter / Drill** | On/Off | Если сегмент **громкий** и **очень короткий** ( с, высокая плотность). | Повторяет первый микро-сегмент 2, 4 или 8 раз. |
| **Flash Frame** | On/Off | Если  и `Chance` прошла. | Вставляет 1-2 кадра белой/черной вспышки для визуального акцента на **перепаде энергии**. |
| **Ghost Trails** | On/Off | Если сегмент **громкий**. | Наложение предыдущего кадра с задержкой 50мс. |
| **Ghost Trail Opacity** | 0.0 – 1.0 | — | Устанавливает прозрачность  для эффекта шлейфа. |

---

## IV. Рабочий процесс и Предпросмотр (Live Preview)

Правая панель обеспечивает итеративный процесс настройки и финального экспорта.

### 1. Контрольная панель

| Элемент | Назначение |
| --- | --- |
| **RENDER PREVIEW** | Запускает генерацию и сохранение **первых 5 секунд** видео в `temp_preview.mp4`. |
| **Live Preview** | Виджет, отображающий циклическое воспроизведение `temp_preview.mp4` через **OpenCV**. |
| **Консоль** | Вывод логов и ошибок (`ANALYSIS COMPLETE`, `RENDERING 5 sec`). |
| **RENDER FULL VIDEO** | Запускает финальный рендеринг видео на полную длину аудиодорожки. |

### 2. Алгоритм итеративной настройки

1. Выберите пресет или настройте параметры.
2. Нажмите **RENDER PREVIEW**.
3. Во время циклического просмотра в плеере, корректируйте параметры в левой панели (например, `Beat Threshold` или `Pixel Sort Intensity`).
4. Повторяйте **RENDER PREVIEW** до достижения желаемого результата.
5. Нажмите **RENDER FULL VIDEO**.